# Git+GitHubの練習コーナー
## 目次
### 1.cd, git add, git status, git commit -m, git restore --staged
### 2.git switch --detach <commit_id>, git switch -c, git branch, git merge, git branch -d(ブランチ名)
### 3.GitHubアカウントの作成、リモートリポジトリ作成
### 4.git remote add origin (連携させたいリモートリポジトリのURL), git remote -v, git push